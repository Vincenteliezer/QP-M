import { MD3LightTheme as DefaultTheme } from 'react-native-paper';

export default theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#010101',
        secondary: '#388E3C',
    },
};